function Jobs_desk() {
    let job = document.querySelectorAll('.jobs');
    const vacancy = [];
    const min = 1;
    const max = 10;
    const randomNum = Math.floor(Math.random() * (max - min + 1)) + min;
}